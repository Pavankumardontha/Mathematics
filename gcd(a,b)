#include<bits/stdc++.h>
#include<iostream>
using namespace std;


/*
  gcd(a,b) =  a          if b=0 
           = gcd(b,a%b)  otherwise 
*/
int gcd(int a,int b)
{
    if(b==0)
    return a;
    else
    return gcd(b,a%b);
}
